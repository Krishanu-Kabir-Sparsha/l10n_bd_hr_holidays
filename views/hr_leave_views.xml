<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- ENHANCED LEAVE REQUEST FORM VIEW -->
    <record id="hr_leave_view_form_enhanced" model="ir.ui.view">
        <field name="name">hr.leave.view.form.enhanced</field>
        <field name="model">hr.leave</field>
        <field name="inherit_id" ref="hr_holidays.hr_leave_view_form"/>
        <field name="priority">50</field>
        <field name="arch" type="xml">
            
            <!-- Add sandwich leave warning -->
            <xpath expr="//sheet" position="inside">
                <div class="alert alert-info" role="alert" invisible="not l10n_bd_contains_sandwich_leaves">
                    <strong>Note:</strong> The requested time considers the sandwich leave policy.
                    Weekends/holidays between leave days are counted.
                </div>
            </xpath>
            
            <!-- Add hidden field for sandwich -->
            <xpath expr="//field[@name='leave_type_request_unit']" position="after">
                <field name="l10n_bd_contains_sandwich_leaves" invisible="1"/>
            </xpath>
            
            <!-- Add quick action buttons -->
            <xpath expr="//header/button[@name='action_approve']" position="before">
                <button string="Quick Approve" 
                        name="action_approve_quick" 
                        type="object" 
                        class="btn-success"
                        invisible="state != 'confirm' or not can_approve"
                        groups="hr_holidays.group_hr_holidays_responsible"/>
            </xpath>
            
            <!-- Add refuse with reason button -->
            <xpath expr="//header/button[@name='action_refuse']" position="after">
                <button string="Refuse with Reason" 
                        name="action_refuse_with_reason" 
                        type="object" 
                        class="btn-warning"
                        invisible="state not in ('confirm', 'validate1', 'validate') or not can_approve"
                        groups="hr_holidays.group_hr_holidays_responsible"/>
            </xpath>
            
            <!-- Add leave category after holiday_status_id -->
            <xpath expr="//field[@name='holiday_status_id']" position="after">
                <field name="l10n_bd_leave_category" readonly="1" widget="badge"/>
            </xpath>
            
        </field>
    </record>
    
    <!-- ENHANCED LEAVE LIST VIEW -->
    <record id="hr_leave_view_tree_enhanced" model="ir.ui.view">
        <field name="name">hr.leave.view.tree.enhanced</field>
        <field name="model">hr.leave</field>
        <field name="inherit_id" ref="hr_holidays.hr_leave_view_tree"/>
        <field name="priority">50</field>
        <field name="arch" type="xml">
            
            <!-- Add leave category column -->
            <xpath expr="//field[@name='holiday_status_id']" position="after">
                <field name="l10n_bd_leave_category" string="Category" widget="badge" optional="show"/>
            </xpath>
            
        </field>
    </record>
    
    <!-- ENHANCED SEARCH VIEW -->
    <record id="hr_leave_view_search_enhanced" model="ir.ui.view">
        <field name="name">hr.leave.view.search.enhanced</field>
        <field name="model">hr.leave</field>
        <field name="inherit_id" ref="hr_holidays.view_hr_holidays_filter"/>
        <field name="priority">50</field>
        <field name="arch" type="xml">
            
            <!-- Add category filters -->
            <xpath expr="//filter[@name='my_leaves']" position="after">
                <separator/>
                <filter string="Annual Leave" name="filter_annual" domain="[('l10n_bd_leave_category', '=', 'annual')]"/>
                <filter string="Casual Leave" name="filter_casual" domain="[('l10n_bd_leave_category', '=', 'casual')]"/>
                <filter string="Sick Leave" name="filter_sick" domain="[('l10n_bd_leave_category', '=', 'sick')]"/>
            </xpath>
            
            <!-- Add group by category -->
            <xpath expr="//filter[@name='group_type']" position="after">
                <filter string="Leave Category" name="group_category" context="{'group_by': 'l10n_bd_leave_category'}"/>
            </xpath>
            
        </field>
    </record>

</odoo>